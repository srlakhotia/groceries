const task = (state, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

const tasks = (state = [], action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default tasks;